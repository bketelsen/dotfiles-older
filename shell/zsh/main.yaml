depends:
  - git

actions:
  - action: file.link
    from: zshrc
    to: "{{ user.home_dir }}/.zshrc"
  - action: file.link
    from: zprofile
    to: "{{ user.home_dir }}/.zprofile"
  - action: file.link
    from: zshenv
    to: "{{ user.home_dir }}/.zshenv"